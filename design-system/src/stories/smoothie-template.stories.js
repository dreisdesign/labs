// Stub to satisfy Storybook's CSF parser.
// This file intentionally exports an empty default so Storybook won't error while
// we keep the legacy source present (no named story exports).

export default {};
